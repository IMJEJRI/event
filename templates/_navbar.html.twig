<nav>
    <ul class="nav_list">
        <li>
            <a href="{{ path('event_listing') }}">Nos Soirées</a>
        </li>
        <li>
            <a href="{{ path('event_listing') }}">Contact</a>
        </li>
        {% if app.user %}
            <li>
                <a href="{{ path('my_events') }}">Mes évènements</a>
            </li>
            <li>
                <a href="{{ path('app_logout') }}">Se déconnecter</a>
            </li>
        {% else %}
            <li>
                <a href="{{ path('app_login') }}">Se connecter</a>
            </li>
            <li>
                <a href="{{ path('app_register') }}">S'inscrire</a>
            </li>
        {% endif %}
    </ul>
</nav>